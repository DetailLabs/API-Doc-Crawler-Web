run = "pip install -q -r requirements.txt && python app.py"
entrypoint = "app.py"
modules = ["python-3.12"]

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "pip install -q -r requirements.txt && python app.py"]

[[ports]]
localPort = 5000
externalPort = 80
